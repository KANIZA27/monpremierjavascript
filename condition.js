// LES CONDITIONS
/*
Si A existe,
alors B s'affiche.
*/

// Pourvoir rédiger une condition j'utilise le mot-cle if() pour créer nue condition
let age = 57
// je veux vérifier si la personne est majeur
if(age > 18 ) {
// alert("la personne est majeur!");
}

// je veux vérifier si la personne est mineure
if(age < 18){
    // alert("la personne est mineure.");
}

// Les consignes
/* Si la note est 5, on affiche insuffisat
Si la note est 10,on affiche moyen
si la note est 12, on affiche assez-bien
Si la note est 15, on affiche bien
Si la note est 18, on affiche très bien
*/

/* Lecture et analyse le besoin
Si la note est EGALE 5, on affiche insuffisat
j'ai besoin d'utiliser une variable nommée note pour stocker la note de l'élèves
Ensuite, je code la condition.
*/

  let note  = 5;
  if(note == 5){
    // alert("insuffisat");
  }

/* Lecture et analyse le besoin
Si la note est EGALE 10, on affiche insuffisat
j'ai besoin d'utiliser une variable nommée note pour stocker la note de l'élèves
Ensuite, je code la condition.
*/

if(note == 10){
  alert("moyen");
}
/* Lecture et analyse le besoin
Si la note est EGALE 12, on affiche insuffisat
j'ai besoin d'utiliser une variable nommée note pour stocker la note de l'élèves
Ensuite, je code la condition.
*/

if(note == 12);{
alert("assez-bien");
}

if(note > 11){//S'il est VRAI que la note est supérieure à 10
  // Alors j'affiche le message "Bravo vous êtes admis!"
    console.log("Bravo! vous êtres admis!");
}else{ // Sinon
//console.log("Désolé! vous n'avez pas été admis.");
}

