// les OPERATEURS
// je vais vous présenter les opérateur en javascript
// les opérateurs il permetter d'effectuer de calculs

// Opérateur + : permte d'additionner de nombre
let prix = 5+5; // Résultat : 10

// Opérateur +permtet de cocaténer des chaînes de caractéres
let message = "Bonjour " + " Madame";

// Opérateur - : permet de soustraire des nombre
let reduction = 10 - 3; // Résultat : 7

// Opérateur * : permt de munltiplier des nombres
let munltiplier = 4*3; // Résultat stocké :54

// Opérateur / : permet de diviser des nombres
let divise = 24/2;

// Opérateur = : permet d' affecter la valeur à la variable en cours

/* Opérateur == : permet de comparer les valeurs entre deux variables
si les deux valeurs sont différentes, 
   alors JavaScript retourne la valeur TRUE
si les deux valeurs sont différentes,
   alors JavaScript retour la valeur
*/

let comparaision = multiplication == division; // Est-ce que 12 =12 ?
/* le signe === vérifier que la valeur soit identique
et le typage soit identique. */
let nombre1 = 12;
let nombre2 = "12";
let tripleegal =nombre1 === nombre2;

/* Opérateur != : permet de vérifier la différence entre deux variables
si les deux valeurs sont différentes, 
   alors JavaScript retourne la valeur TRUE
si les deux valeurs sont différentes,
   alors JavaScript retour la valeur
*/


let différent = multiplication != division;
alert(tripleegal);