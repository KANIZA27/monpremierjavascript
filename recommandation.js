// LES SYSTEME DE RECOMMANDATION
let ageUtilisateur =19;
if( ageUtilisateur>18){//Si sutilisateur à pus de 18 ans
 }
if(ageUtilisateur < 13){// si l'utilisateur à moins de 13 ans, on affiche "Roi lion"
console.log("le roi lion") // Alors on lui affiche 'le roi lion'
};

if(ageUtilisateur > 13 && ageUtilisateur < 18 ) { // si l'utilisateur a plus de 13 ans et moins de 18 ans.
 console.log("Titanic") // Alors on lui affiche "Titanic"
};

let ticke = {
    nomFilm: "koungou",
    prix: 7,
    numeroSalle:15,
}
let nom = "Assan"
// Avec console.log() je afficher le nom du film et le numero de salle
console.log(" Bonjour et bienvenue", nom, ticke.nomFilm, ticke.numeroSalle );
